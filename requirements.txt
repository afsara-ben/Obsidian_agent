# Core dependencies
requests>=2.31.0   # HTTP client for Ollama REST API
Pillow>=10.0.0     # Image processing (resize, JPEG conversion)

# Optional: For Gemini API fallback (alternative to Ollama)
# google-generativeai>=0.3.0
